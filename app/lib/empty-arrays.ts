import type { CloudJobSummary } from './replay-cloud/types';

/** Stable empty array for default props (avoids new reference each render). */
export const EMPTY_CLOUD_JOBS: CloudJobSummary[] = [];

/** Stable empty array for default props (avoids new reference each render). */
export const EMPTY_STRING_ARRAY: string[] = [];
